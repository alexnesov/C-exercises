#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{

    
    struct Result {
    int max;
    int min;
    };

    Result f(vector<int> t);
    
    void f (vector<int> t, int& max , int& min);

} 